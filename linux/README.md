# DESCRIPTION 

The script `totalsize.sh` is used to show the total size of the current directory (files + sub-directories).

It just runs the unix commands 'du -h | tail -1'

Don't forget to do chmod +x totalsize.sh, before using it.
